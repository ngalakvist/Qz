@model QuizApp.Models.AnswerChoice

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>AnswerChoice</legend>

        @Html.HiddenFor(model => model.AnswerChoiceId)

        <div class="editor-label">
            @Html.LabelFor(model => model.Choices)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Choices)
            @Html.ValidationMessageFor(model => model.Choices)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.QuestionId, "Question")
        </div>
        <div class="editor-field">
            @Html.DropDownList("QuestionId", String.Empty)
            @Html.ValidationMessageFor(model => model.QuestionId)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
